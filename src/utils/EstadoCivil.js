export default function GetEstadoCivil() {
    const estadoCivil = [
        'solteiro(a)',
        'casado(a)',
        'viúvo(a)',
        'separado(a) judicialmente',
        'divorciado(a)']

    return estadoCivil;
}